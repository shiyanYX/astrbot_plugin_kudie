# CONTEXT.md

领域词汇表。`grill-with-docs` 和其他工程技能会参照本文档中的术语。

## 核心概念

- **尽孝文章** — 插件生成的游戏深度评论文章，以"把游戏公司当父亲尽孝"的戏谑口吻吹捧游戏。由用户提供游戏名和事件描述，经 LLM 生成。

- **事件** — 用户提供的游戏相关热点或争议，如"新角色节奏""版本更新服务器崩了""有人黑角色"。是文章生成的核心素材。

- **角色** — 游戏中的人物。文章生成时需要角色的基本信息：姓名、性别、年龄、角色类型（如"冷艳御姐""热血少年"）。数据来源于 wiki，缓存 7 天。

- **写作风格** — 9 种内置 + DIY 自定义。通过 `style_manager.py` 管理，本质是注入到 prompt 中的风格修饰词。

## 信息获取

- **搜索链路** — 由 `/尽孝搜索` 触发的完整流程：LLM 搜索词优化 → 并行搜索（贴吧/B站 → 事件，wiki → 角色）→ LLM 加工背景摘要 → LLM 写文章。耗时较长但信息准确。

- **普通链路** — 由 `/尽孝` 触发的简化流程：用缓存中的角色数据 + 纯 LLM 常识生成。快速、不触发外部搜索。

- **搜索词优化** — 将用户输入转成更好的搜索词。默认由 LLM 提取关键词，可在配置中切换为"直接使用原文"。

- **信息源** — 三个外部数据源：
  - **贴吧** — 通过百度站内搜索 + Cookie 认证，抓取标题、热帖正文、评论
  - **B站** — 通过百度站内搜索，抓取视频标题、简介、热门评论
  - **Wiki** — 按游戏自动选择（米哈游→B站Wiki，FGO→Mooncell，通用→萌娘百科），可在配置中覆盖

- **Cookie 凭证** — 用户通过 `/尽孝登录` 命令写入的贴吧/B站 Cookie，用于获取需要登录才能看到的完整内容。

## 缓存

- **事件缓存** — 同一"游戏+事件描述"的搜索结果短期缓存（默认 10 分钟，可配置），避免重复搜索。

- **角色缓存** — 角色基本信息长期缓存（默认 7 天），key 为 `游戏名_角色名`，存储为 `data/character_cache.json`。可通过 `/尽孝缓存 清除` 手动清理。

## 容错

- **降级** — 爬虫失败或超时时的处理链：缓存兜底 → 纯 LLM 生成（无外部信息），同时提示"⚠️ 未获取到最新信息"。

- **反爬** — UA 伪装 + 随机请求间隔（1-3 秒）+ 指数退避重试（最多 3 次，1s/2s/4s），超过次数降级到纯 LLM 模式。
